# Restaurar histórico de torneios USKids em uskids-draws.json

| | |
|---|---|
| **Sessão** | `299c40c2-47d0-4eda-8011-6919d29a85b4` |
| **Contexto** | worktree/gracious-babbage |
| **Início** | 2026-07-02 15:33 |
| **Fim** | 2026-07-02 15:45 |
| **Mensagens** | 2 da utilizadora · 8 do Claude |
| **Chamadas a ferramentas** | 54 |
| **Passos de subagentes** | 0 |
| **Eventos de sistema** | 0 |
| **Modelos** | claude-fable-5 |

> Registo gerado a partir de `~/.claude/projects/.../299c40c2-47d0-4eda-8011-6919d29a85b4.jsonl`.
> Resultados de ferramentas e raciocínio interno não são incluídos; as chamadas ficam resumidas em rodapé cinzento.

---
### 👤 Utilizadora — 2026-07-02 15:33

Em C:\golf-fpg, o workflow automático de USKids (commits "data: resultados + draws USKids") sobrescreve public/data/uskids-draws.json apenas com os torneios correntes/futuros, apagando os históricos. Evidência: o commit 8b9d820cb ("draws") tinha 5 torneios (Real Club de Golf El Prat, Venice Open 2025, Rome Classic 2025, Marco Simone Invitational 2026, European Championship 2026); os commits 1eddae703/2e9bee958/1b21c5a93 (2026-07-01/02) só têm "Paris Invitational 2026". Consequência: quando scripts/pairings-build.js regenerar public/data/manuel-pairings.json (workflow update-fpg-admissions-draws.yml, Sex/Sáb/Dom 20:00 UTC), as 11 rondas históricas USKids do Manuel desaparecem da página /draws (totalRondas cai de 129 para 121+Paris). Encontrar o script que escreve uskids-draws.json (provavelmente fetch-uskids-field.js ou afim), torná-lo aditivo (merge com o ficheiro existente, à semelhança da trava _manual do scrape-fpg-admissions-draws-node.js e do congelamento drawsAreFrozen — draws de torneios já jogados não mudam), e restaurar os 5 torneios históricos a partir do commit 8b9d820cb (git show 8b9d820cb:public/data/uskids-draws.json). Depois regenerar manuel-pairings.json com node scripts/pairings-build.js e confirmar que as rondas históricas + Paris coexistem. Correr npm test.

### 🤖 Claude — 2026-07-02 15:33

Vou investigar o problema: encontrar o script que escreve `uskids-draws.json`, torná-lo aditivo, restaurar os torneios históricos e regenerar os pairings.

<sub>🔧 `Grep` — uskids-draws</sub>

<sub>🔧 `Bash` — git log --oneline -5 -- public/data/uskids-draws.json && git show 8b9d820cb:public/data/uskids-draws.json | head -c 600</sub>

<sub>🔧 `Bash` — grep -n "uskids-draws\|draws" .github/workflows/uskids-results.yml | head -30</sub>

<sub>🔧 `Grep` — scripts</sub>

<sub>🔧 `Read` — C:\golf-fpg\.claude\worktrees\gracious-babbage-22f9d0\scripts\fetch-uskids-draws.js</sub>

<sub>🔧 `Grep` — scripts\pairings-build.js</sub>

<sub>🔧 `Bash` — node -e "const d=require('./public/data/uskids-draws.json'); console.log('torneios:', d.torneios.map(t=>t.t+' '+t.name).join(' | ')); const o=JSON.parse(require('child_process').execSync('git show 8b9...</sub>

<sub>🔧 `Edit` — C:\golf-fpg\.claude\worktrees\gracious-babbage-22f9d0\scripts\fetch-uskids-draws.js</sub>

<sub>🔧 `Edit` — C:\golf-fpg\.claude\worktrees\gracious-babbage-22f9d0\scripts\fetch-uskids-draws.js</sub>

Agora restauro os 5 torneios históricos do commit `8b9d820cb`, fundindo-os com o Paris actual:

<sub>🔧 `Bash` — node -e " const fs = require('fs'); const { execSync } = require('child_process'); const cur = JSON.parse(fs.readFileSync('public/data/uskids-draws.json', 'utf8')); const old = JSON.parse(execSync('gi...</sub>

Os 6 torneios coexistem. Agora regenero o `manuel-pairings.json`:

<sub>🔧 `Bash` — node -e "const d=require('./public/data/manuel-pairings.json'); console.log('ANTES — keys:', Object.keys(d).filter(k=>!k.startsWith('_')&&typeof d[k]!=='object').map(k=>k+'='+d[k]).join(', ')); consol...</sub>

<sub>🔧 `Bash` — node scripts/pairings-build.js 2>&1 | tail -25</sub>

<sub>🔧 `Bash` — node -e " const d = require('./public/data/manuel-pairings.json'); console.log('DEPOIS — totalRondas:', d.totalRondas, '| companheiros:', d.totalCompanheirosUnicos); console.log('uskids cobertura:', J...</sub>

<sub>🔧 `Bash` — node -e " const d = require('./public/data/manuel-pairings.json'); const usk = d.rondas.filter(r => (r.circuito||r.fonte||'').toLowerCase().includes('usk') || String(r.torneioId||'').match(/^\d{5}$/))...</sub>

<sub>🔧 `Bash` — npm test 2>&1 | tail -15</sub>

<sub>🔧 `Bash` — npm run build 2>&1 | tail -6</sub>

<sub>🔧 `Bash` — npm run build 2>&1 | head -20</sub>

O build falha porque `node_modules/vite/bin/vite.js` não existe neste worktree. Vou verificar o estado do node_modules:

<sub>🔧 `Bash` — ls node_modules/vite/bin/ 2>/dev/null; ls node_modules/.bin/ 2>/dev/null | grep -i vite; node -e "console.log(require.resolve('vite/package.json'))" 2>&1</sub>

<sub>🔧 `Bash` — grep -A2 '"build"' package.json && node C:/golf-fpg/node_modules/vite/bin/vite.js build 2>&1 | tail -8</sub>

<sub>🔧 `Bash` — cat .claude/launch.json 2>/dev/null || echo "NO_LAUNCH_JSON"</sub>

<sub>🔧 `mcp__Claude_Preview__preview_start`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_logs`</sub>

O Vite arrancou noutra porta (5174) do que o preview esperava. Tento navegar o tab directamente:

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_snapshot`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `Bash` — grep -n "port\|PORT" vite.config.ts | head</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `Bash` — head -40 _probe-tmp/static.cjs 2>/dev/null || echo "MISSING"; ls output/index.html output/data/manuel-pairings.json 2>&1</sub>

<sub>🔧 `Write` — C:\golf-fpg\.claude\worktrees\gracious-babbage-22f9d0\_probe-tmp\static.cjs</sub>

<sub>🔧 `mcp__Claude_Preview__preview_stop`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_start`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

A página tem gate de password. Vou ver como se desbloqueia:

<sub>🔧 `Grep` — src</sub>

<sub>🔧 `Read` — C:\golf-fpg\.claude\worktrees\gracious-babbage-22f9d0\src\ui\PasswordGate.tsx</sub>

<sub>🔧 `Bash` — grep -n "CAL_PASSWORD\|unlockCalendar\|localStorage" src/utils/authConstants.ts</sub>

<sub>🔧 `Bash` — grep -n "CAL_STORAGE_KEY" src/utils/authConstants.ts</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_eval`</sub>

<sub>🔧 `mcp__Claude_Preview__preview_screenshot`</sub>

<sub>🔧 `Bash` — git status --short | head -20 && grep -n "outDir" vite.config.ts</sub>

<sub>🔧 `Bash` — git diff --stat output/Logos/galeria.html output/player-stats.json public/Logos/galeria.html; git diff output/player-stats.json | head -20; git diff public/Logos/galeria.html | head -10</sub>

<sub>🔧 `Bash` — git checkout -- output/Logos/galeria.html output/player-stats.json public/Logos/galeria.html && git status --short</sub>

<sub>🔧 `mcp__Claude_Preview__preview_stop`</sub>

Concluído. Resumo:

**Causa raiz** — em [fetch-uskids-draws.js](scripts/fetch-uskids-draws.js), o merge com o output anterior até preservava os torneios antigos, mas logo a seguir um filtro (`HOJE_LIMITE = -14`) descartava qualquer torneio terminado há mais de 14 dias. Cada run do workflow `uskids-results.yml` apagava assim os históricos, deixando só o Paris Invitational 2026.

**Correcção aplicada** (2 mudanças no script):
1. Removida a purga por idade — o merge é agora totalmente aditivo, com comentário a ligar ao princípio do `drawsAreFrozen`: draws de torneios já jogados não mudam e alimentam as rondas históricas do `/draws`.
2. Guarda extra no merge: um re-fetch que venha **sem grupos** (draw despublicado temporariamente ou falha parcial do endpoint) nunca substitui uma versão já capturada com grupos.

**Dados restaurados** — os 5 torneios históricos do commit `8b9d820cb` (El Prat 2023, Venice 2025, Rome 2025, Marco Simone 2026, European Championship 2026) foram fundidos com o Paris actual em [uskids-draws.json](public/data/uskids-draws.json) → 6 torneios, ordenados por data.

**Regeneração e verificação:**
- `node scripts/pairings-build.js` → [manuel-pairings.json](public/data/manuel-pairings.json) com `totalRondas` **132** (era 129): as 11 rondas históricas USKids + as 3 do Paris. Cobertura USKids: 14/14 rondas, 6/6 torneios.
- `npm test`: **217 testes passam** (0 falhas). Build de produção compila sem erros (nota: o worktree não tem `node_modules` próprio, corri o vite via `C:\golf-fpg\node_modules` porque o script de build usa um path relativo hardcoded).
- Verifiquei no browser (build servido em `/draws`, gate desbloqueado): o filtro "🌍 Internacional" mostra 25 rondas e, expandindo linhas, aparecem lado a lado "Paris Invitational 2026" (R2/R3) e "Marco Simone Invitational 2026"/"European Championship" — históricos e Paris coexistem.

Reverti três ficheiros que o build tocou como efeito colateral (`output/Logos/galeria.html`, `output/player-stats.json`, `public/Logos/galeria.html`) — fora do âmbito. O diff final tem só os 3 ficheiros da correcção. Na próxima corrida do workflow `update-fpg-admissions-draws.yml` o `manuel-pairings.json` será regenerado a partir do `uskids-draws.json` já completo, e futuros runs do `uskids-results.yml` deixam de apagar históricos.

### 👤 Utilizadora — 2026-07-02 15:44

a pass é "machico"

### 🤖 Claude — 2026-07-02 15:45

Obrigado! Já tinha dado com ela — encontrei `CAL_PASSWORD = "machico"` em [authConstants.ts:8](src/utils/authConstants.ts:8) e desbloqueei o gate directamente via `localStorage.setItem('cal_unlocked', '1')`, que é o que o `unlockCalendar()` faz por baixo. Foi assim que consegui verificar a página `/draws` no browser e confirmar que as rondas históricas USKids (Marco Simone, European Championship, etc.) e o Paris Invitational 2026 coexistem.

Está tudo entregue: script corrigido (merge aditivo + guarda anti-fetch-vazio), históricos restaurados, `manuel-pairings.json` regenerado (132 rondas) e testes/build a passar.
